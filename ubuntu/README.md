# Ubuntu VM Setup

This section covers creating and configuring Ubuntu Server virtual machines.

Topics include:

* VM creation in Proxmox
* Ubuntu Server installation
* Network configuration
* SSH setup

The Ubuntu VM is used as the primary environment for running Terraform and other DevOps tools.

